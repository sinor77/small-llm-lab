{
  "experiment_name": "exp_03_curriculum",
  "description": "Curriculum learning: train first on easy problems, then medium, then hard. Same total examples as baseline.",
  "output_dir": "experiments/results/exp_03_curriculum",
  "seed": 42,
  "model_size": "8m",
  "n_train": 20000,
  "n_val": 2000,
  "n_test": 1000,
  "difficulty": "medium",
  "use_reasoning": true,
  "data_seed": 42,
  "tokenizer_vocab_size": 4096,
  "max_epochs": 30,
  "max_steps": -1,
  "batch_size": 64,
  "grad_accumulation_steps": 2,
  "eval_every_n_steps": 500,
  "save_every_n_steps": 1000,
  "log_every_n_steps": 50,
  "eval_n_problems": 500,
  "optimizer": "adamw",
  "learning_rate": 3e-4,
  "weight_decay": 0.1,
  "beta1": 0.9,
  "beta2": 0.95,
  "grad_clip": 1.0,
  "scheduler": "cosine",
  "warmup_steps": 200,
  "min_lr_ratio": 0.1,
  "use_amp": true,
  "max_seq_len": 256,
  "gen_max_new_tokens": 64,
  "gen_temperature": 0.0,
  "resume_from": null,
  "_notes": "Curriculum phasing must be implemented in a custom train script. This config is a placeholder for the experiment record."
}
